/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://calculator.proskilledu.com',
  generateRobotsTxt: true, // Generates robots.txt file
  // Optionally exclude some routes
  // exclude: ['/private-page'],
};
